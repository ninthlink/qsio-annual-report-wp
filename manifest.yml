applications:
- name: dev-qsio-wp-alex
  instances: 1
  mem: 256M
  services:
    ${name}-db:
      type: mysqlha
    ${name}-fs:
      type: filesystem
  stackato:
    hooks:
      post-staging:
      # setting up in filesystem
      - sh stackato-setup.sh
